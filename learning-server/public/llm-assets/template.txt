<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>Assessment Template</title>
  <style>
    body {
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      margin: 0;
      padding: 40px;
      background: #ffffff;
      color: #1f2937;
      line-height: 1.6;
    }

    .container {
      max-width: 920px;
      margin: 0 auto;
    }

    h1 {
      font-size: 26px;
      color: #2563eb;
      margin-bottom: 14px;
    }

    .divider {
      height: 1px;
      background: #d1d5db;
      margin: 24px 0 28px;
    }

    h2 {
      font-size: 20px;
      margin-top: 32px;
      margin-bottom: 12px;
    }

    h3 {
      font-size: 18px;
      margin-top: 22px;
      margin-bottom: 6px;
    }

    p {
      margin: 0 0 14px;
    }

    .callout {
      background: #eef6ff;
      /* BLUE box */
      border-radius: 8px;
      padding: 16px 18px;
      margin: 14px 0 20px;
    }

    .callout strong {
      font-weight: 600;
    }

    .icon {
      margin-right: 6px;
    }

    .callout-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 16px;
      margin: 12px 0 20px;
    }

    .callout--concept {
      background: #eaf4ff;
    }

    .callout--explanation {
      background: #fff1f5;
    }

    @media (max-width: 720px) {
      .callout-grid {
        grid-template-columns: 1fr;
      }
    }

    .section {
      margin-bottom: 28px;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>{{Icon"💡"}}{{PAGE_TITLE_: e.g. "Assessment"}}</h1>

    <div class="divider"></div>

    <div class="section">
      <!-- BLUE BOX for sections or a concpet(single sample only) -->
      <div class="callout">
        <span class="icon">{{BLUE_BOX_ICON_: e.g. "🧾"}}</span>
        <strong>{{BLUE_BOX_TITLE_: e.g. "Evidence / Example"}}</strong><br />
        {{BLUE_BOX_BODY_:
        }}
      </div>

      <div class="callout-grid">
        <div class="callout callout--concept">
          <span class="icon">{{CONCEPT_BOX_ICON_: e.g. "💡"}}</span>
          <strong>{{CONCEPT_BOX_TITLE_: e.g. "Concept"}}</strong><br />
          {{CONCEPT_BOX_BODY_:
          ONE short paragraph describing the key idea in general terms.}}
        </div>

        <div class="callout callout--explanation">
          <span class="icon">{{EXPLANATION_BOX_ICON_: e.g. "🧭"}}</span>
          <strong>{{EXPLANATION_BOX_TITLE_: e.g. "Explanation"}}</strong><br />
          {{EXPLANATION_BOX_BODY_:
          ONE short paragraph explaining the idea clearly and simply.}}
        </div>
      </div>

      <!-- ORANGE BOX (single sample only) -->
      <div class="callout" style="background:#fff4e5;">
        <h3 style="margin-top:0;">
          <span class="icon">{{ORANGE_BOX_ICON_: e.g. "✅"}}</span>
          {{ORANGE_BOX_TITLE_: e.g. "Summary"}}
        </h3>
        <p style="margin-bottom:0;">
          {{ORANGE_BOX_BODY_:
          1–2 sentences giving the overall summary in topic-agnostic language.
          Use <strong> once for the main emphasis.}}
        </p>
      </div>
    </div>
  </div>
</body>

</html>